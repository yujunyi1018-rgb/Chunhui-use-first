# 春晖门业官网运营维护手册

适用网站目录：`网站上传`

本手册面向日常运营、内容编辑、产品资料维护和上线检查人员。当前网站是静态网站，没有后台管理系统，主要通过修改 HTML、JS 配置文件和图片资源来更新内容。

---

## 1. 网站结构总览

### 1.1 核心页面

| 页面 | 文件位置 | 主要用途 |
| --- | --- | --- |
| 首页 | `index.html` | 公司展示、产品入口、优势、流程、FAQ、联系入口 |
| 产品中心 | `product-center.html` | 全部产品列表、分类筛选、产品图册入口 |
| 产品详情 | `product-detail.html` | 单个产品详情、大图、参数、适用场景、相关推荐 |
| 关于我们 | `guanyuwomen/index.html` | 公司介绍、服务范围、发展信息、常见问答 |
| 新闻动态 | `xinwendongtai/index.html` | 新闻、案例、产品知识、选购指南 |
| 联系我们 | `lianxiwomen/index.html` | 联系方式、询盘表单、客户需求提交 |

### 1.2 核心配置文件

| 文件 | 作用 | 日常是否常改 |
| --- | --- | --- |
| `data.js` | 首页、关于我们、新闻案例、联系信息、导航等内容数据 | 经常 |
| `product-manifest.js` | 产品中心和产品详情页的完整产品数据 | 经常 |
| `ziyuan/main.js` | 首页渲染逻辑 | 一般不改 |
| `ziyuan/product-center.js` | 产品中心筛选、图册、列表逻辑 | 少改 |
| `ziyuan/product-detail.js` | 产品详情页逻辑 | 少改 |
| `ziyuan/news-cases.js` | 新闻案例页渲染逻辑 | 少改 |
| `ziyuan/contact.js` | 联系页和表单提交逻辑 | 偶尔 |
| `ziyuan/style.css` | 首页、关于、新闻、联系等页面样式 | 偶尔 |
| `ziyuan/product-center.css` | 产品中心样式 | 偶尔 |
| `ziyuan/product-detail.css` | 产品详情样式 | 偶尔 |

### 1.3 图片资源目录

| 目录 | 用途 |
| --- | --- |
| `ziyuan/images/` | Logo、首页 Banner、二维码等公共图片 |
| `ziyuan/icons/` | 网站图标，例如 `favicon.png` |
| `xinwendongtai/` | 新闻动态配图，例如 `news_01.jpg` |
| `chanpinzhanshi/` | 产品图片总目录 |
| `chanpinzhanshi/kaoqimen/shimukaoqimen/` | 实木烤漆门图片（含5个子系列目录：bolimen、mupigongyimen、pingdiaomen、shishangbaimen、xiangmumen） |
| `chanpinzhanshi/kaoqimen/fuhekaiqimen/` | 复合烤漆门图片（待上线） |
| `chanpinzhanshi/wuqimen/lvmumen/` | 铝木门相关图片 |
| `chanpinzhanshi/wuqimen/tanjingmen/` | 碳晶门图片（待上线） |

---

## 2. 维护前准备

### 2.1 推荐工具

1. 文本编辑器：VS Code、Notepad++ 或其他支持 UTF-8 的编辑器。
2. 图片处理工具：Photoshop、稿定设计、Canva 或系统自带图片工具。
3. 浏览器：Chrome 或 Edge，用于本地预览和上线检查。
4. 压缩工具：用于打包上传网站文件。

### 2.2 修改前必须备份

每次改网站前，先复制一份完整目录，例如：

```text
网站上传_备份_2026-06-05
```

至少备份这些文件：

```text
data.js
product-manifest.js
index.html
product-center.html
product-detail.html
guanyuwomen/index.html
xinwendongtai/index.html
lianxiwomen/index.html
ziyuan/
chanpinzhanshi/
```

### 2.3 编码注意事项

所有页面声明为 UTF-8：

```html
<meta charset="UTF-8">
```

编辑时必须用 UTF-8 保存。不要用会自动改编码的旧版记事本或办公软件直接编辑 JS/HTML，否则中文可能变乱码。

### 2.4 修改 JS 数据的基本规则

`data.js` 和 `product-manifest.js` 都是 JavaScript 数据文件，修改时要注意：

1. 字符串用英文双引号或单引号包住。
2. 每一项之间用英文逗号 `,` 分隔。
3. 不要漏掉 `{}`、`[]`、`,`、引号。
4. 路径使用正斜杠 `/`，不要写成 Windows 反斜杠 `\`。
5. 图片路径必须相对网站根目录，例如：

```js
image: "chanpinzhanshi/kaoqimen/shimukaoqimen/xiangmumen/产品图片.jpg"
```

---

## 3. 怎么更新内容

本章是日常运营最常用部分。更新内容通常分为文字、图片、产品、新闻案例、联系信息和 SEO 信息。

### 3.1 更新公司名称、电话、邮箱、地址

主要修改文件：`data.js`

找到：

```js
company: {
  name: "...",
  displayName: "...",
  phone: "...",
  mobile: "...",
  email: "...",
  secondaryEmail: "...",
  address: "...",
  branchAddress: "...",
  contactPerson: "...",
  wechat: "...",
  icp: "...",
},
```

根据实际情况修改对应字段。

需要同步检查的页面：

1. 首页底部联系方式。
2. 关于我们页底部。
3. 联系我们页联系方式。
4. `index.html` 里的结构化数据电话、邮箱和地址。

如果只改 `data.js`，大多数展示内容会更新；但 `index.html` 头部的 SEO 和结构化数据里也写了部分公司信息，建议同步更新。

### 3.2 更新首页统计数据

主要修改文件：`data.js`

找到：

```js
stats: [
  { number: "2012年", label: "创立时间" },
  { number: "500+", label: "全国合作门店" },
]
```

修改 `number` 和 `label` 即可。

注意事项：

1. 数字尽量短，避免卡片撑开。
2. 不确定的数据不要写得过满，例如可写 `500+`、`多年`、`批量`。
3. 改完后打开首页检查统计区是否换行正常。

### 3.3 更新首页精选产品

主要修改文件：`data.js`

首页展示的精选产品来自：

```js
products: [
  {
    id: 1,
    name: "...",
    series: "...",
    desc: "...",
    tag: "...",
    price: "...",
    image: "...",
    accent: "#8B3A2A",
  },
]
```

字段说明：

| 字段 | 说明 |
| --- | --- |
| `id` | 首页精选产品编号，保持唯一 |
| `name` | 产品名称 |
| `series` | 产品系列，必须能和筛选分类对应 |
| `desc` | 简短描述 |
| `tag` | 卡片角标，例如“热销款”“工程适配” |
| `price` | 价格展示，通常写“询价定制” |
| `image` | 产品图片路径 |
| `accent` | 占位背景色，图片加载失败时显示 |

更新步骤：

1. 将新图片放入对应产品目录。
2. 复制图片相对路径。
3. 在 `data.js` 的 `products` 数组中修改对应产品。
4. 打开 `index.html` 检查首页产品卡片。
5. 点击“查看完整产品中心”，确认产品中心也能正常打开。

### 3.4 更新产品中心完整产品库

主要修改文件：`product-manifest.js`

产品中心和产品详情页主要读取 `PRODUCT_MANIFEST`：

```js
const PRODUCT_MANIFEST = [
  {
    "title": "玻璃门系列CHA-B01_1",
    "description": "玻璃门系列CHA-B01_1",
    "category": "实木烤漆门",
    "series": "烤漆门",
    "folder": "kaoqimen/shimukaoqimen",
    "image": "chanpinzhanshi/kaoqimen/shimukaoqimen/玻璃门系列CHA-B01_1.jpg"
  }
]
```

字段说明：

| 字段 | 说明 |
| --- | --- |
| `title` | 产品标题，产品中心和详情页会显示 |
| `description` | 产品描述，当前多用于搜索和详情摘要 |
| `category` | 产品大类，例如”实木烤漆门””铝木门” |
| `series` | 产品系列，例如”橡木门””玻璃门””平雕门””木皮工艺门””时尚白门””铝木门” |
| `folder` | 图片所在分类目录，不含 `chanpinzhanshi/` 前缀 |
| `image` | 图片完整相对路径 |

更新已有产品步骤：

1. 打开 `product-manifest.js`。
2. 搜索要更新的产品标题或图片文件名。
3. 修改 `title`、`description`、`category`、`series` 或 `image`。
4. 打开 `product-center.html`，搜索该产品名称。
5. 点击产品进入详情页，确认大图、标题、分类、相关推荐正常。

### 3.5 更新产品图片

如果只替换图片，不改产品名：

1. 准备新图片，建议使用 `.jpg`、`.png` 或 `.webp`。
2. 图片文件名尽量清晰，例如 `橡木深雕门系列CHA-97_1.jpg`。
3. 放到原产品所在目录。
4. 如果新图片沿用旧文件名，直接覆盖旧图即可。
5. 如果新图片使用新文件名，要同步修改 `product-manifest.js` 里的 `image`。

建议图片规范：

| 类型 | 建议 |
| --- | --- |
| 产品图 | 竖图或接近竖图，主体居中，背景干净 |
| 首页 Banner | 横图，宽度建议 1920px 以上 |
| 新闻配图 | 横图，宽度建议 1200px 以上 |
| 文件大小 | 单张建议控制在 300KB 到 1.5MB，避免过大影响加载 |

### 3.6 更新新闻动态

主要修改文件：`data.js`

新闻列表来自：

```js
news: [
  {
    title: "...",
    date: "...",
    category: "...",
    summary: "...",
    image: "xinwendongtai/news_01.jpg",
  },
]
```

更新步骤：

1. 把新闻图片放入 `xinwendongtai/`。
2. 打开 `data.js`。
3. 找到 `news` 数组。
4. 修改标题、日期、分类、摘要和图片路径。
5. 打开 `xinwendongtai/index.html` 检查新闻卡片。

新闻分类来自：

```js
newsCategories: ["全部", "品牌动态", "产品知识", "选购指南", "工程案例"],
```

如果新闻使用新分类，要把分类名同步加入 `newsCategories`。

### 3.7 更新案例内容

主要修改文件：`data.js`

案例来自：

```js
caseStudies: [
  {
    title: "...",
    type: "...",
    scene: "...",
    desc: "...",
    tags: ["...", "..."],
    image: "...",
  },
]
```

更新步骤：

1. 准备案例图片，放到 `xinwendongtai/` 或其他合适目录。
2. 修改 `caseStudies` 中对应项目。
3. 确认 `tags` 是数组格式，例如 `["酒店公寓", "批量交付"]`。
4. 打开新闻动态页检查案例区域。

### 3.8 更新关于我们内容

主要修改文件：`data.js`

关于我们主要来自：

```js
about: {
  title: "...",
  lead: "...",
  content: "...",
  image: "...",
  serviceScope: [],
  targetClients: [],
  quickFacts: [],
  proofPoints: [],
  highlights: [],
  timeline: [],
}
```

常见更新：

1. 公司介绍改 `title`、`lead`、`content`。
2. 公司图片改 `image`。
3. 服务范围改 `serviceScope`。
4. 客户类型改 `targetClients`。
5. 发展历程改 `timeline`。
6. 制造实力、交付优势改 `highlights` 或 `proofPoints`。

检查页面：

1. 首页“关于春晖”板块。
2. `guanyuwomen/index.html` 关于我们详情页。

### 3.9 更新 FAQ 常见问题

主要修改文件：`data.js`

FAQ 来自：

```js
faqs: [
  {
    question: "...",
    answer: "...",
  },
]
```

更新步骤：

1. 找到 `faqs`。
2. 修改问题和回答。
3. 首页和新闻动态页都会使用 FAQ 数据，改完要两个页面都检查。

写法建议：

1. 问题要像客户真实会问的话。
2. 回答控制在 80 到 180 字。
3. 尽量包含产品名、服务对象、交付能力等关键词。

### 3.10 更新联系方式页面表单收件邮箱

主要修改文件：`lianxiwomen/index.html`

找到：

```html
<form id="contact-inquiry-form" action="https://formsubmit.co/ajax/邮箱地址" method="POST">
```

把邮箱地址换成新的收件邮箱。

同时建议修改 `data.js` 中：

```js
company.email
company.secondaryEmail
contact.salesPhone
contact.mobilePhone
```

检查方式：

1. 打开 `lianxiwomen/index.html`。
2. 填写测试姓名和电话。
3. 提交表单。
4. 确认收件邮箱能收到询盘。

注意：FormSubmit 第一次使用新邮箱时，可能需要到邮箱里点击确认邮件。

### 3.11 更新导航菜单

主要修改文件：`data.js`

首页导航来自：

```js
nav: [
  { label: "首页", href: "index.html" },
  { label: "产品中心", href: "product-center.html" },
]
```

更新步骤：

1. 修改 `label` 可改变显示文字。
2. 修改 `href` 可改变跳转地址。
3. 如果新增导航项，按相同格式增加一行。

注意：

1. 首页使用根路径写法，例如 `product-center.html`。
2. 子页面一般使用 `../` 返回上级，例如 `../index.html`。
3. 当前子页面顶部导航有部分是写死在 HTML 里，如果统一改导航文案，也要检查 `product-center.html`、`guanyuwomen/index.html`、`xinwendongtai/index.html`、`lianxiwomen/index.html`。

### 3.12 更新 SEO 标题、描述和关键词

主要修改文件：

```text
index.html
product-center.html
product-detail.html
guanyuwomen/index.html
xinwendongtai/index.html
lianxiwomen/index.html
```

每个页面头部都有：

```html
<meta name="description" content="...">
<meta name="keywords" content="...">
<title>...</title>
```

更新建议：

1. `title` 控制在 20 到 35 个中文字符左右。
2. `description` 控制在 70 到 120 个中文字符左右。
3. `keywords` 用英文逗号分隔。
4. 每个页面不要完全一样，要贴合页面主题。

---

## 4. 怎么增加内容

本章说明如何新增产品、新闻、案例、图册、页面和入口。

### 4.1 新增产品到产品中心

新增产品需要同时准备图片和数据。

步骤：

1. 确认产品属于哪个目录。

   示例：

   ```text
   实木烤漆门-橡木门：chanpinzhanshi/kaoqimen/shimukaoqimen/xiangmumen/
   实木烤漆门-平雕门：chanpinzhanshi/kaoqimen/shimukaoqimen/pingdiaomen/
   实木烤漆门-玻璃门：chanpinzhanshi/kaoqimen/shimukaoqimen/bolimen/
   实木烤漆门-木皮工艺门：chanpinzhanshi/kaoqimen/shimukaoqimen/mupigongyimen/
   实木烤漆门-时尚白门：chanpinzhanshi/kaoqimen/shimukaoqimen/shishangbaimen/
   铝木门：chanpinzhanshi/wuqimen/lvmumen/
   ```

2. 把产品图片放入对应目录。

3. 打开 `product-manifest.js`。

4. 在 `PRODUCT_MANIFEST` 数组中增加一项：

```js
{
  "title": "新产品名称",
  "description": "新产品名称或简短描述",
  "category": "实木烤漆门",
  "series": "橡木门",
  "folder": "kaoqimen/shimukaoqimen/xiangmumen",
  "image": "chanpinzhanshi/kaoqimen/shimukaoqimen/xiangmumen/新产品名称.jpg"
},
```

5. 保存文件。

6. 打开 `product-center.html` 检查：

   - 产品总数是否增加。
   - 分类筛选是否能找到。
   - 搜索产品名称是否能找到。
   - 点击进入详情页是否正常。
   - 图片是否正常显示。

常见错误：

| 问题 | 原因 | 解决 |
| --- | --- | --- |
| 产品不显示 | JSON/JS 格式错误 | 检查逗号、引号、大括号 |
| 图片不显示 | 路径或文件名不一致 | 复制实际文件名重新粘贴 |
| 分类筛选异常 | `category` 写错或不统一 | 使用已有分类名 |
| 详情页打开空白 | 数据项格式错误 | 对照上一条产品数据检查 |

### 4.2 新增产品到首页精选

如果新产品也要出现在首页产品区，还要修改 `data.js` 的 `products`。

示例：

```js
{
  id: 7,
  name: "新产品名称",
  series: "实木烤漆门",
  desc: "适合现代家装、工程配套和整装项目的室内门产品。",
  tag: "新品推荐",
  price: "询价定制",
  image: "chanpinzhanshi/kaoqimen/shimukaoqimen/xiangmumen/新产品名称.jpg",
  accent: "#314B43",
},
```

注意：

1. `id` 不要和已有产品重复。
2. 首页不宜放太多产品，建议保持 6 到 8 个精选。
3. 首页产品图片也要保证路径正确。

### 4.3 新增产品分类

如果新增一个全新分类，例如“工程门”，需要考虑两个地方。

第一处：`product-manifest.js`

新增产品时写新的分类：

```js
"category": "工程门",
"series": "工程门",
```

产品中心会根据 `product-manifest.js` 自动统计分类。

第二处：`ziyuan/product-center.js`

如果希望分类卡片有专门说明，需要在 `CATEGORY_META` 增加：

```js
"工程门": {
  desc: "适合酒店、公寓、精装房和批量工程项目，重点关注规格统一、稳定交付和售后响应。",
  fit: "批量交付 / 项目配套 / 规格统一",
},
```

第三处：`ziyuan/product-detail.js`

如果希望产品详情页也有专门说明，同样在 `CATEGORY_META` 增加对应分类说明。

第四处：首页筛选分类

如果首页精选产品也要支持这个分类，修改 `data.js`：

```js
productCategories: ["全部", "实木烤漆门", "铝木门"],
```

### 4.4 新增新闻动态

步骤：

1. 准备新闻标题、日期、分类、摘要和图片。
2. 图片放到 `xinwendongtai/`。
3. 打开 `data.js`。
4. 找到 `news`。
5. 在数组最前面增加一条，这样新内容会排在前面：

```js
{
  title: "新新闻标题",
  date: "2026-06-05",
  category: "品牌动态",
  summary: "这里写新闻摘要，建议 60 到 120 字，说明事件、产品或客户价值。",
  image: "xinwendongtai/news_04.jpg",
},
```

6. 如果用了新分类，修改 `newsCategories`。
7. 打开 `xinwendongtai/index.html` 检查。

运营建议：

1. 新闻标题尽量包含关键词，例如”实木烤漆门””铝木门””工程门””中山门业”。
2. 摘要不要只写口号，要写具体信息。
3. 新闻图片要和内容相关。

### 4.5 新增工程案例

步骤：

1. 准备案例名称、项目类型、场景、描述、标签、图片。
2. 图片放入 `xinwendongtai/` 或单独新建案例图片目录。
3. 打开 `data.js`。
4. 找到 `caseStudies`。
5. 增加一项：

```js
{
  title: "某酒店公寓室内门配套案例",
  type: "工程案例",
  scene: "酒店公寓",
  desc: "围绕统一风格、批量供货、交付节点和后续补货需求，提供室内门产品配套建议。",
  tags: ["酒店公寓", "批量交付", "工程配套"],
  image: "xinwendongtai/case_01.jpg",
},
```

6. 打开新闻动态页检查案例卡片。

### 4.6 新增常见问题

步骤：

1. 打开 `data.js`。
2. 找到 `faqs`。
3. 增加一项：

```js
{
  question: "工程项目可以按尺寸定制吗？",
  answer: "可以。可根据项目类型、门洞尺寸、数量、风格、五金配置和交付时间沟通定制方案，适用于酒店、公寓、精装房和批量工程项目。",
},
```

4. 打开首页和新闻动态页检查 FAQ。

### 4.7 新增电子图册

主要修改文件：`ziyuan/product-center.js`

产品中心的电子图册来自：

```js
const BROCHURES = [
  {
    title: "产品图册 01",
    desc: "适合快速浏览整体款式、门型与风格方向。",
    url: "https://book.yunzhan365.com/...",
  },
]
```

新增步骤：

1. 准备图册外链。
2. 打开 `ziyuan/product-center.js`。
3. 在 `BROCHURES` 数组中增加一项。
4. 打开 `product-center.html`，检查图册按钮和 iframe 是否能加载。

注意：

1. 外链必须是可公开访问的 HTTPS 链接。
2. 如果外链平台禁止 iframe 嵌入，页面中可能无法预览，但“新窗口打开”仍可使用。

### 4.8 新增 Banner 或公共图片

步骤：

1. 把图片放入 `ziyuan/images/`。
2. 如果替换已有 Banner，可以用同名文件覆盖，例如 `banner_01.jpg`。
3. 如果使用新文件名，需要到引用该图片的 CSS 或 JS 中修改路径。

常见位置：

1. 首页视觉图：`ziyuan/style.css` 或 `data.js` 中的图片字段。
2. Logo：`ziyuan/images/logo-mark.png`。
3. 二维码：`ziyuan/images/qrcode.png`、`ziyuan/images/qrcode2.png`。

### 4.9 新增独立页面

如果要增加一个新页面，例如“工程合作”。

推荐步骤：

1. 新建目录：

```text
gongchenghezuo/
```

2. 在目录中新建：

```text
gongchenghezuo/index.html
```

3. 可复制 `guanyuwomen/index.html` 或 `xinwendongtai/index.html` 作为模板。
4. 修改页面标题、描述、主体内容。
5. 如需要独立 JS，新建：

```text
ziyuan/project-page.js
```

6. 如需要独立样式，可优先复用 `ziyuan/style.css`。
7. 在 `data.js` 的 `nav` 中增加入口。
8. 检查所有链接路径，子页面引用根目录资源通常要加 `../`。

路径示例：

```html
<link rel="stylesheet" href="../ziyuan/style.css">
<script src="../data.js"></script>
```

---

## 5. 内容编辑规范

### 5.1 产品名称规范

建议格式：

```text
系列名称 + 型号 + 颜色或材质
```

示例：

```text
国潮墨影系列 S039 冰山白
木皮工艺门系列 CHA-129
M9系列 同色铝木门
```

### 5.2 新闻标题规范

推荐标题方向：

1. 产品知识：`碳晶门适合哪些空间？家装和工程选型要点`
2. 选购指南：`实木烤漆门和铝木门怎么选？`
3. 工程案例：`酒店公寓室内门批量配套需要关注哪些问题？`
4. 品牌动态：`春晖门业产品体系持续完善，服务多类室内门需求`

### 5.3 图片命名规范

建议：

```text
分类_系列_型号_颜色_序号.jpg
```

示例：

```text
实木烤漆门_橡木深雕_CHA97_01.jpg
烤漆门_CHA129_木皮工艺_01.jpg
新闻_工程门选型指南_01.jpg
```

避免：

1. 文件名只有 `1.jpg`、`新建图片.jpg`。
2. 文件名包含大量特殊符号。
3. 文件名和产品名称完全不相关。

---

## 6. 上线发布流程

### 6.1 本地检查

每次修改后至少打开这些页面：

```text
index.html
product-center.html
product-detail.html?id=0
guanyuwomen/index.html
xinwendongtai/index.html
lianxiwomen/index.html
```

检查清单：

1. 页面能打开，没有空白。
2. 中文没有乱码。
3. 图片正常显示。
4. 导航能跳转。
5. 产品中心筛选和搜索可用。
6. 产品详情页可打开。
7. 联系表单按钮可点击。
8. 手机宽度下布局不严重错位。

### 6.2 上传服务器

静态网站上线时，一般上传整个网站目录内容。

需要上传：

```text
*.html
data.js
product-manifest.js
ziyuan/
chanpinzhanshi/
guanyuwomen/
xinwendongtai/
lianxiwomen/
```

上传注意：

1. 保持目录结构不变。
2. 不要只上传 HTML，JS 和图片也要同步上传。
3. 如果只是改产品图片，可以只上传新图片和 `product-manifest.js`。
4. 如果改了 `data.js`，必须上传新的 `data.js`。
5. 上传后清浏览器缓存再检查。

### 6.3 上线后检查

上线后用正式网址检查：

1. 首页标题和内容。
2. 产品中心产品数量。
3. 新增产品图片。
4. 新闻动态是否显示新内容。
5. 联系表单是否能提交。
6. 手机端是否正常。
7. 浏览器控制台是否有明显报错。

---

## 7. 常见故障处理

### 7.1 页面打开后内容不显示

可能原因：

1. `data.js` 格式写错。
2. `product-manifest.js` 格式写错。
3. JS 文件没有上传。
4. 浏览器缓存旧文件。

处理方式：

1. 回想最近改了哪个文件。
2. 检查是否漏逗号、引号、大括号。
3. 用备份文件恢复，确认页面恢复后再重新小步修改。
4. 按 `Ctrl + F5` 强制刷新。

### 7.2 图片不显示

可能原因：

1. 图片没有上传。
2. 路径写错。
3. 文件名大小写不一致。
4. 文件扩展名写错，例如实际是 `.png`，代码里写 `.jpg`。
5. 文件名含特殊字符，服务器识别异常。

处理方式：

1. 复制图片实际文件名。
2. 对照 `image` 路径逐级检查目录。
3. 尽量使用简洁文件名。
4. 重新上传图片。

### 7.3 产品筛选找不到新产品

可能原因：

1. 新产品没有加入 `product-manifest.js`。
2. `category` 或 `series` 写错。
3. JS 格式错误导致后续数据未加载。

处理方式：

1. 搜索产品标题确认是否存在。
2. 对照同类产品复制一条再改。
3. 打开产品中心搜索产品关键词。

### 7.4 联系表单收不到邮件

可能原因：

1. `form action` 邮箱写错。
2. 新邮箱没有完成 FormSubmit 确认。
3. 邮件进入垃圾箱。
4. 网络或第三方服务异常。

处理方式：

1. 检查 `lianxiwomen/index.html` 中的 `form action`。
2. 登录收件邮箱查找 FormSubmit 确认邮件。
3. 检查垃圾邮件。
4. 页面上保留电话和微信，避免表单异常时客户无法联系。

### 7.5 中文变乱码

可能原因：

1. 文件不是 UTF-8 保存。
2. 编辑器自动改了编码。
3. 上传服务器时编码被转换。

处理方式：

1. 用 VS Code 打开文件。
2. 右下角确认编码为 UTF-8。
3. 如果不是 UTF-8，另存为 UTF-8。
4. 从备份恢复乱码前版本。

---

## 8. 例行维护计划

### 每周

1. 检查首页、产品中心、联系页能否正常打开。
2. 测试联系表单。
3. 检查是否有图片加载失败。
4. 查看是否有过期电话、邮箱、地址。

### 每月

1. 更新 1 到 2 条新闻、产品知识或案例。
2. 检查产品中心是否有下架、缺图、命名不清的产品。
3. 压缩过大的图片。
4. 备份整站。

### 每季度

1. 更新首页精选产品。
2. 检查 SEO 标题和描述是否符合当前业务重点。
3. 检查图册链接是否仍可打开。
4. 检查所有二维码是否仍有效。
5. 汇总客户常问问题，补充 FAQ。

---

## 9. 推荐改动顺序

为了减少出错，建议按这个顺序改：

1. 先备份。
2. 先放图片。
3. 再改数据文件。
4. 再改页面标题或 SEO。
5. 本地打开检查。
6. 上传服务器。
7. 正式网址复查。

不要一次同时大改很多地方。运营内容可以小步更新，每次只改一个模块，确认没问题再继续。

---

## 10. 快速定位表

| 我要做的事 | 修改哪里 |
| --- | --- |
| 改电话、邮箱、地址 | `data.js` 的 `company` 和 `contact`，必要时同步 `index.html` 结构化数据 |
| 改首页产品 | `data.js` 的 `products` |
| 改完整产品库 | `product-manifest.js` |
| 加产品图片 | `chanpinzhanshi/` 对应分类目录 |
| 改新闻 | `data.js` 的 `news` |
| 加新闻分类 | `data.js` 的 `newsCategories` |
| 改案例 | `data.js` 的 `caseStudies` |
| 改 FAQ | `data.js` 的 `faqs` |
| 改关于我们 | `data.js` 的 `about` |
| 改电子图册链接 | `ziyuan/product-center.js` 的 `BROCHURES` |
| 改表单收件邮箱 | `lianxiwomen/index.html` 的 `form action` |
| 改 SEO 标题描述 | 各页面 HTML 的 `<title>`、`description`、`keywords` |
| 改样式 | `ziyuan/style.css`、`ziyuan/product-center.css`、`ziyuan/product-detail.css` |

---

## 11. 最小安全修改示例

### 11.1 只换一个产品图片

1. 把新图片命名为和旧图片一样。
2. 覆盖原目录旧图片。
3. 打开产品中心检查。
4. 不需要改 JS。

### 11.2 新增一条新闻

1. 上传新闻图到 `xinwendongtai/news_04.jpg`。
2. 在 `data.js` 的 `news` 最前面加一条。
3. 打开新闻动态页检查。

### 11.3 新增一个完整产品

1. 上传产品图到对应 `chanpinzhanshi/` 子目录。
2. 在 `product-manifest.js` 增加产品对象。
3. 打开产品中心搜索产品。
4. 点击进入详情页检查。

---

## 12. 交接建议

如果后续有多人维护，建议建立一个简单交接表：

| 日期 | 修改人 | 修改内容 | 修改文件 | 是否备份 | 是否上线检查 |
| --- | --- | --- | --- | --- | --- |
| 2026-06-11 | — | 产品更新：移除生态门系列，实木烤漆门拆分5子系列，产品总数188款 | `data.js`、`data-en.js`、`product-manifest.js`、各页面HTML | 是 | 是 |

每次上线后保留最近 3 到 5 个备份版本，方便出问题时快速恢复。
